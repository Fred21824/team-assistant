# Team Assistant 配置文件 - 服务器版本

Server:
  Port: 8090
  Mode: release

# 数据库配置 - 服务器本地连接
MySQL:
  Host: "127.0.0.1:3306"
  User: "root"
  Password: "123456"
  Database: "team_assistant"

# Redis 配置 - 服务器本地连接
Redis:
  Host: "127.0.0.1:6379"
  Password: "BSIRedis2025"
  DB: 5  # 使用独立的 DB，避免与其他服务冲突

# 飞书配置
Lark:
  # 国际版用 larksuite.com，国内版用 feishu.cn
  Domain: "https://open.larksuite.com"
  AppID: "cli_a8810599b478d028"
  AppSecret: "A6mNclZDCu0f9qNZ1FnEkb2rXv3pk2f5"
  # 事件订阅的验证 Token（在飞书开放平台配置后填入）
  VerificationToken: ""
  # 事件加密 Key（可选，开启加密后填入）
  EncryptKey: ""
  # 机器人的 open_id（用于判断是否@机器人，启动后通过API获取）
  BotOpenID: ""

# GitHub 配置
GitHub:
  # Personal Access Token
  Token: "your_github_token_here"
  # Webhook Secret（可选，用于验证 GitHub 推送）
  WebhookSecret: ""
  # 监控的组织/用户
  Organizations:
    - "bsibw"

# LLM 配置 - 使用 Claude (通过美国代理访问)
LLM:
  Provider: "anthropic"
  APIKey: "your_anthropic_api_key_here"
  Endpoint: "https://api.anthropic.com/v1/messages"
  Model: "claude-haiku-4-5-20251001"
  # 美国 HTTP 代理服务器配置（香港无法直接访问 Claude API）
  ProxyHost: "52.41.128.82"
  ProxyPort: 9662
  ProxyUser: "user"
  ProxyPassword: "CNikMeB2sVP3tLVUB"

# Dify 配置（暂时禁用）
Dify:
  Enabled: false
  BaseURL: "http://127.0.0.1:8088"
  APIKey: "dataset-33a61459e58e61f1bb6790bbd833aacf7b1e63f8"
  DatasetID: "577ce425-df32-4f51-a7e7-cb0404e56a2a"

# 向量数据库配置（用于语义搜索 RAG）
VectorDB:
  Enabled: true
  QdrantEndpoint: "http://127.0.0.1:6333"  # Qdrant 向量数据库地址（本地）
  OllamaEndpoint: "http://127.0.0.1:11434"  # Ollama embedding 服务地址（本地）
  EmbeddingModel: "nomic-embed-text"
  CollectionName: "lark_messages"

# Bitable（多维表格）配置 - 站点信息表
Bitable:
  Enabled: true
  AppToken: "DMGPbsyU4azQpmsUFgcl8UKHgnh"
  TableID: "tblbJLbc7ioORThL"
